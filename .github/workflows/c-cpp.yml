name: C/C++ CI

on:
  push

jobs:
  build:

    runs-on: ${{ matrix.os }}
  
    strategy:
      matrix:
        os: [macOS-latest, ubuntu-latest]

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: cmake . -B build
    - name: make
      run: cmake --build build
